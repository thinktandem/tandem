name: docs-tandem
excludes:
  - node_modules
proxy:
  node:
    - docs-tandem.lndo.site:8080
services:
  node:
    type: node:10
    build:
      - yarn install
    command: "su - node -c 'cd /app && yarn dev'"
    port: 8080
tooling:
  node:
    service: node
  yarn:
    service: node
  mdl:
    service: node
    cmd: markdownlint
  vuepress:
    service: node
  test:
    service: node
    cmd: yarn run test
    description: Run tests
